import styled from 'styled-components'

export const Container = styled.div`
    display: flex;
    flex-direction: row;
    align-items: center;
    height: 70px;

    a {
        font-size: 0.88rem;
        text-decoration: none;
        color: inherit;
    }
`
